# 🖥️ UniTrade 前端部署设计方案

> **版本**: V1.0 | **日期**: 2026-07-07 | **作者**: 产品技术团队

---

## 一、技术选型与理由

| 技术 | 版本 | 角色 | 选型理由 |
|------|------|------|----------|
| **React** | 19 | UI框架 | 生态最完善，Server Components 原生支持 |
| **Next.js** | 15 (App Router) | 全栈框架 | SSR/SSG/ISR 灵活组合，SEO 友好，API Routes 可做 BFF |
| **TypeScript** | 5.x | 类型系统 | 大型项目必备，接口即文档 |
| **TailwindCSS** | 4.x | 样式方案 | 原子化CSS，与 shadcn/ui 深度绑定，设计令牌天然统一 |
| **shadcn/ui** | latest | 组件库 | 复制源码而非安装依赖，完全可控，无障碍(WAI-ARIA)内置 |
| **Zustand** | 5.x | 客户端状态 | 轻量(1KB)，无 boilerplate，胜过 Redux/Context 组合 |
| **TanStack Query** | 5.x | 服务端状态 | 缓存/去重/重试/乐观更新开箱即用 |
| **Axios** | 1.x | HTTP客户端 | 拦截器链、取消请求、进度上报，比 fetch 更适合 BFF 通信 |
| **React Hook Form** | 7.x | 表单管理 | 非受控组件性能最优，配合 Zod 做类型安全校验 |
| **Zod** | 3.x | Schema校验 | TypeScript-first，前后端共享类型定义 |
| **Recharts** | 2.x | 图表库 | React 原生，声明式 API，轻量 |
| **next-intl** | 4.x | 国际化 | 支持 App Router、SSR、按需加载语言包 |
| **Lucide React** | latest | 图标库 | 与 shadcn/ui 默认配套，Tree-shakable |

---

## 二、项目目录结构

```
client/
├── .github/
│   └── workflows/
│       ├── ci.yml                    # PR检查：lint + type-check + test
│       └── deploy.yml                # 合并到main自动部署
│
├── public/
│   ├── favicon.ico
│   ├── og-image.png                  # Open Graph 默认图
│   └── locales/                      # 静态语言包（如需要离线访问）
│
├── src/
│   ├── app/                          # === Next.js App Router ===
│   │   ├── (auth)/                   # 认证页组（无侧边栏布局）
│   │   │   ├── login/
│   │   │   │   └── page.tsx
│   │   │   ├── register/
│   │   │   │   └── page.tsx
│   │   │   └── layout.tsx            # 居中卡片式布局
│   │   │
│   │   ├── (dashboard)/             # 主应用页组（含侧边栏+顶栏布局）
│   │   │   ├── layout.tsx            # DashboardLayout（含Sidebar + Topbar）
│   │   │   ├── page.tsx              # 首页 = CEO仪表盘
│   │   │   ├── products/
│   │   │   │   ├── page.tsx          # 商品列表
│   │   │   │   ├── [id]/
│   │   │   │   │   └── page.tsx      # 商品详情
│   │   │   │   └── ai-listing/
│   │   │   │       └── page.tsx      # AI Listing 生成器
│   │   │   ├── orders/
│   │   │   │   ├── page.tsx          # 订单列表
│   │   │   │   └── [id]/
│   │   │   │       └── page.tsx      # 订单详情
│   │   │   ├── analytics/
│   │   │   │   └── page.tsx          # 数据分析
│   │   │   └── settings/
│   │   │       ├── page.tsx          # 店铺管理
│   │   │       └── team/
│   │   │           └── page.tsx      # 团队管理
│   │   │
│   │   ├── api/                      # BFF API Routes
│   │   │   ├── auth/
│   │   │   │   └── [...nextauth]/    # NextAuth.js 路由
│   │   │   ├── ai/
│   │   │   │   └── generate-listing/ # AI Listing 生成代理
│   │   │   └── webhooks/
│   │   │       └── amazon/           # Amazon SQS 通知接收
│   │   │
│   │   ├── globals.css               # Tailwind + CSS变量
│   │   ├── layout.tsx                # 根布局（HTML > Body）
│   │   └── providers.tsx             # QueryClient + Theme + Auth Provider
│   │
│   ├── components/                   # === 组件（按功能域分层）===
│   │   ├── ui/                       # shadcn/ui 基础组件（Button/Input/...）
│   │   ├── layout/                   # 布局组件
│   │   │   ├── sidebar.tsx
│   │   │   ├── topbar.tsx
│   │   │   ├── breadcrumb.tsx
│   │   │   └── command-palette.tsx   # Cmd+K 全局搜索
│   │   ├── auth/                     # 认证相关
│   │   │   ├── login-form.tsx
│   │   │   └── register-form.tsx
│   │   ├── products/                 # 商品模块
│   │   │   ├── product-table.tsx
│   │   │   ├── product-card.tsx
│   │   │   ├── listing-generator/
│   │   │   │   ├── step-input.tsx
│   │   │   │   ├── step-generate.tsx
│   │   │   │   ├── step-editor.tsx
│   │   │   │   └── step-publish.tsx
│   │   │   └── listing-preview.tsx
│   │   ├── orders/                   # 订单模块
│   │   │   ├── order-table.tsx
│   │   │   ├── order-detail.tsx
│   │   │   └── batch-label.tsx
│   │   ├── dashboard/                # 仪表盘模块
│   │   │   ├── stat-card.tsx
│   │   │   ├── sales-chart.tsx
│   │   │   └── top-products.tsx
│   │   └── shared/                   # 跨模块通用
│   │       ├── data-table.tsx        # 通用数据表格
│   │       ├── filter-bar.tsx
│   │       ├── empty-state.tsx
│   │       └── loading-skeleton.tsx
│   │
│   ├── lib/                          # === 工具与配置 ===
│   │   ├── api/                      # API客户端
│   │   │   ├── client.ts             # Axios 实例 + 拦截器
│   │   │   ├── products.ts           # 商品API
│   │   │   ├── orders.ts             # 订单API
│   │   │   └── analytics.ts          # 分析API
│   │   ├── auth.ts                   # NextAuth 配置
│   │   ├── validators/               # Zod Schema（前后端共享）
│   │   │   ├── product.ts
│   │   │   ├── order.ts
│   │   │   └── auth.ts
│   │   ├── constants.ts              # 常量
│   │   ├── utils.ts                  # 通用工具函数
│   │   └── font.ts                   # 字体配置
│   │
│   ├── hooks/                        # === 自定义Hooks ===
│   │   ├── use-products.ts
│   │   ├── use-orders.ts
│   │   ├── use-dashboard.ts
│   │   ├── use-ai-listing.ts
│   │   └── use-media-query.ts
│   │
│   ├── stores/                       # === Zustand Stores ===
│   │   ├── sidebar-store.ts
│   │   ├── user-store.ts
│   │   └── app-store.ts              # 全局UI状态（主题/语言/...）
│   │
│   ├── types/                        # === TypeScript 类型 ===
│   │   ├── product.ts
│   │   ├── order.ts
│   │   ├── dashboard.ts
│   │   └── api.ts                    # 统一API响应类型
│   │
│   └── i18n/                         # === 国际化 ===
│       ├── messages/
│       │   ├── en.json
│       │   └── zh.json
│       ├── config.ts
│       └── request.ts
│
├── tests/                            # === 测试 ===
│   ├── unit/                         # Vitest 单元测试
│   └── e2e/                          # Playwright E2E
│
├── .env.local.example                # 环境变量模板
├── components.json                   # shadcn/ui 配置
├── tailwind.config.ts
├── tsconfig.json
├── next.config.ts
├── vitest.config.ts
├── playwright.config.ts
├── Dockerfile                        # 多阶段构建
├── docker-compose.yml                # 本地开发环境
├── nginx.conf                        # 生产反向代理
└── package.json
```

---

## 三、组件架构设计

### 3.1 组件分层原则

```
┌──────────────────────────────────────────────┐
│                  页面层 (Pages)                │  ← App Router page.tsx
│              编排布局+数据获取+传递Props         │
├──────────────────────────────────────────────┤
│                功能组件层 (Features)            │  ← 业务逻辑+状态+API调用
│         product-table / listing-generator     │
├──────────────────────────────────────────────┤
│                 通用组件层 (Shared)             │  ← 无业务逻辑，纯展示+交互
│              data-table / filter-bar          │
├──────────────────────────────────────────────┤
│                基础组件层 (UI Kit)              │  ← shadcn/ui (Button/Input/...)
│                                               │
└──────────────────────────────────────────────┘
```

### 3.2 核心页面组件树

```
RootLayout (app/layout.tsx)
├── Providers (QueryClient + Theme + Auth)
│
├── (auth)/layout.tsx ── AuthLayout
│   ├── login/page.tsx ── LoginPage > LoginForm
│   └── register/page.tsx ── RegisterPage > RegisterForm
│
└── (dashboard)/layout.tsx ── DashboardLayout
    ├── Sidebar
    │   ├── Logo
    │   ├── NavItems (商品/订单/分析/设置...)
    │   └── UserMenu
    ├── Topbar
    │   ├── Breadcrumb
    │   ├── SearchBox (⌘K)
    │   └── NotificationBell
    └── <main> {children}
        │
        ├── page.tsx ── DashboardPage
        │   ├── StatCard × 4 (销售额/订单/转化率/广告花费)
        │   ├── SalesChart (Recharts 折线图)
        │   └── TopProducts (Table)
        │
        ├── products/page.tsx ── ProductListPage
        │   ├── FilterBar
        │   ├── DataTable (SKU/标题/价格/库存/状态)
        │   └── Pagination
        │
        ├── products/ai-listing/page.tsx ── AIListingPage
        │   └── StepWizard
        │       ├── Step1-InputForm (产品信息表单)
        │       ├── Step2-Generating (AI 生成中动画)
        │       ├── Step3-Editor (富文本编辑+评分)
        │       └── Step4-Publish (发布确认)
        │
        └── orders/page.tsx ── OrderListPage
            ├── FilterBar (状态/日期/店铺)
            ├── BatchActions (批量打单/发货)
            └── DataTable
```

---

## 四、状态管理架构

### 4.1 数据流设计

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   Browser    │     │   Next.js BFF    │     │   Backend APIs  │
│   (Client)   │     │   (Server)       │     │   (Microservices│
└──────┬───────┘     └────────┬─────────┘     └────────┬────────┘
       │                      │                        │
  ┌────┴─────────┐     ┌─────┴──────────┐      ┌──────┴──────┐
  │ Zustand      │     │ Server Actions │      │ gRPC/REST   │
  │ (UI State)   │     │ + API Routes   │ ───► │ Auth/Product│
  │              │     │                │      │ /Order/...  │
  │ • 侧边栏折叠  │     │ • Session验证   │      └─────────────┘
  │ • Toast消息  │     │ • Token透传     │
  │ • 当前店铺   │     │ • 数据聚合      │
  └──────┬───────┘     │ • CSRF保护      │
         │             └────────┬─────────┘
  ┌──────┴───────┐             │
  │ TanStack     │◄────────────┘
  │ Query        │
  │ (Server State│
  │  Cache)      │
  │              │
  │ • 商品列表    │
  │ • 订单数据    │
  │ • 仪表盘数据  │
  │ • AI生成结果  │
  └──────────────┘
```

### 4.2 状态分类

| 状态类型 | 管理工具 | 示例 | 持久化 |
|----------|----------|------|--------|
| **服务端数据** | TanStack Query | 商品列表、订单、报表 | 缓存5min，stale-while-revalidate |
| **UI状态** | Zustand | 侧边栏折叠、弹窗显隐 | 内存 |
| **用户偏好** | Zustand + localStorage | 语言、主题、默认店铺 | localStorage |
| **表单状态** | React Hook Form | 搜索框、AI Listing表单 | 不持久化 |
| **URL状态** | Next.js useSearchParams | 分页、筛选、排序 | URL Query String |
| **认证状态** | NextAuth.js Session | Token、用户信息 | httpOnly Cookie |

---

## 五、路由与导航设计

### 5.1 路由表

| 路径 | 页面 | 布局 | 权限 | SSR策略 |
|------|------|------|------|---------|
| `/login` | 登录 | AuthLayout | 公开 | SSR (禁用) |
| `/register` | 注册 | AuthLayout | 公开 | SSR (禁用) |
| `/` | CEO仪表盘 | DashboardLayout | 已登录 | ISR 5min |
| `/products` | 商品列表 | DashboardLayout | 已登录 | SSR |
| `/products/[id]` | 商品详情 | DashboardLayout | 已登录 | SSR |
| `/products/ai-listing` | AI Listing | DashboardLayout | 已登录 | CSR |
| `/orders` | 订单列表 | DashboardLayout | 已登录 | SSR |
| `/orders/[id]` | 订单详情 | DashboardLayout | 已登录 | SSR |
| `/analytics` | 数据分析 | DashboardLayout | 已登录 | ISR 5min |
| `/settings` | 设置 | DashboardLayout | 已登录 | CSR |
| `/settings/team` | 团队管理 | DashboardLayout | Admin | CSR |

### 5.2 导航结构

```
侧边栏 (Sidebar)
├── 📊 仪表盘          → /
├── 🛍️ 商品管理         → /products
│   └── 🤖 AI Listing  → /products/ai-listing
├── 📦 订单管理         → /orders
├── 📈 数据分析         → /analytics
└── ⚙️ 设置            → /settings
    └── 👥 团队管理     → /settings/team

顶栏 (Topbar)
├── 🍞 面包屑导航
├── ⌘K 全局搜索/命令面板
└── 🔔 通知中心
```

---

## 六、认证与授权方案

### 6.1 认证流程

```
用户 → 登录表单 → BFF API → Backend Auth Service → JWT返回
         │                                              │
         ▼                                              ▼
   NextAuth.js                               httpOnly Cookie
   Session管理                                 存储 Refresh Token
         │
         ▼
   Middleware (middleware.ts)
   拦截 /(dashboard)/* 路由
   无Session → 重定向 /login
   有Session → 刷新Token（如需要）→ 放行
```

### 6.2 中间件配置

```typescript
// middleware.ts 核心逻辑
export const config = {
  matcher: ['/(dashboard)/:path*', '/api/:path*']
}
// 1. 检查 Session 有效性
// 2. 无 Session → /login
// 3. Token 即将过期 → 后台刷新
// 4. 通过 X-User-Id / X-Tenant-Id Header 透传给后端
```

---

## 七、API 层设计

### 7.1 BFF 模式

```
Browser ──► Next.js BFF ──► Backend Services

为什么用 BFF？
• 隐藏后端微服务细节，前端只和 BFF 通信
• 数据聚合：一个页面调用 BFF 的 1 个端点，BFF 内部聚合约 3-5 个微服务
• Token 安全：Access Token 只在服务端，浏览器只有 httpOnly Cookie
• 协议转换：前端 HTTP/JSON → BFF → 后端 gRPC
```

### 7.2 Axios 客户端配置

```typescript
// lib/api/client.ts 设计

const apiClient = axios.create({
  baseURL: '/api',     // 全部走 BFF，不直接调后端
  timeout: 15000,
})

// 请求拦截器——自动注入租户上下文
apiClient.interceptors.request.use((config) => {
  // 从 Cookie 读取 tenant-id，注入 Header
})

// 响应拦截器——统一错误处理
apiClient.interceptors.response.use(
  (res) => res.data,
  (error) => {
    // 401 → 跳转登录
    // 403 → Toast "无权限"
    // 429 → Toast "操作频繁，请稍候"
    // 5xx → Toast "服务器异常"
  }
)
```

### 7.3 API 端点清单（客户端视角）

| 分类 | 端点 | 方法 | 说明 |
|------|------|------|------|
| 认证 | `/api/auth/*` | - | NextAuth 托管 |
| 商品 | `/api/products` | GET | 商品列表（分页/筛选/搜索） |
| 商品 | `/api/products/:id` | GET | 商品详情 |
| 商品 | `/api/products/ai/generate` | POST | AI Listing 生成（SSE流式） |
| 订单 | `/api/orders` | GET | 订单列表 |
| 订单 | `/api/orders/:id` | GET | 订单详情 |
| 订单 | `/api/orders/batch-label` | POST | 批量生成运单 |
| 订单 | `/api/orders/batch-ship` | POST | 批量标记发货 |
| 分析 | `/api/analytics/dashboard` | GET | 仪表盘数据 |
| 分析 | `/api/analytics/reports/sales` | GET | 销售报表 |

---

## 八、性能优化策略

| 策略 | 实现 | 目标 |
|------|------|------|
| **代码分割** | Next.js 自动按 Route 分割 + `dynamic()` 懒加载重组件 | 首页 JS < 150KB |
| **图片优化** | `next/image` + WebP/AVIF 自动转换 + 懒加载 | LCP < 2.5s |
| **字体优化** | `next/font` 子集化 + `display: swap` | 无布局偏移(CLS < 0.1) |
| **预取** | `<Link prefetch>` 视口内自动预取 | 页面切换感知延迟 < 200ms |
| **缓存** | TanStack Query staleTime 5min + ISR | 仪表盘秒开 |
| **虚拟列表** | `@tanstack/react-virtual` 处理大列表(>1000行) | 列表渲染 < 50ms |
| **SSR/SSG/ISR** | 按页面特性选择最适策略 | FCP < 1.5s |
| **Bundle分析** | `@next/bundle-analyzer` 定期审计 | 总 Bundle < 300KB gzip |

---

## 九、部署方案

### 9.1 部署架构

```
                           ┌──────────────┐
                           │   CloudFront  │  ← CDN (静态资源 + ISR)
                           └──────┬───────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
              ┌─────┴─────┐              ┌──────┴──────┐
              │   S3      │              │   ALB       │
              │ (Static)  │              │ (App Tier)  │
              └───────────┘              └──────┬──────┘
                                               │
                                    ┌──────────┴──────────┐
                                    │                     │
                              ┌─────┴─────┐         ┌─────┴─────┐
                              │ ECS x N   │         │ ECS x M   │
                              │ Next.js   │         │ Next.js   │
                              │ (SSR)     │         │ (SSR)     │
                              └───────────┘         └───────────┘
```

### 9.2 部署流程 (GitOps)

```
Git Push (main) → GitHub Actions → Build Docker Image → Push ECR
                                                              │
                                                              ▼
                                          ArgoCD 检测到 Image Tag 变更
                                                              │
                                                              ▼
                                          Rolling Update (ECS / K8s)
                                          新 Pod 健康检查通过 → 流量切换
                                          旧 Pod 等待连接排空 → 终止
```

### 9.3 环境配置

| 环境 | 域名 | 部署方式 | 自动部署 |
|------|------|----------|----------|
| **Preview** | `pr-{N}.preview.unitrade.io` | Vercel / 临时ECS | 每个 PR 自动创建 |
| **Staging** | `staging.unitrade.io` | ECS (单实例) | Push to `develop` |
| **Production** | `app.unitrade.io` | ECS × 3 (多AZ) | Push to `main` (审批后) |

### 9.4 Dockerfile（多阶段构建）

```dockerfile
# Stage 1: 依赖安装
FROM node:24-alpine AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev

# Stage 2: 构建
FROM node:24-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

# Stage 3: 运行
FROM node:24-alpine AS runner
WORKDIR /app
RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs
COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
USER nextjs
EXPOSE 3000
CMD ["node", "server.js"]
```

---

## 十、实施计划

### Phase 0 — 基础骨架（第 1-2 周）

```
目标：项目跑起来，路由通，能登录

□ Day 1-2  脚手架搭建
  - create-next-app + TypeScript + TailwindCSS
  - 安装核心依赖（Zustand / TanStack Query / Axios / shadcn/ui）
  - 配置 ESLint + Prettier + Husky(pre-commit)

□ Day 3-5  布局系统
  - AuthLayout (login/register页)
  - DashboardLayout (Sidebar + Topbar + 内容区)
  - shadcn/ui 基础组件初始化 (Button/Input/Card/Dropdown/...)
  - 响应式断点验证

□ Day 6-8  认证系统
  - NextAuth.js 配置 (Credentials + Google OAuth)
  - Login/Register 页面 + 表单校验
  - Middleware 路由守卫
  - Session 管理 + Token 刷新

□ Day 9-10 路由与导航
  - 所有路由占位页面
  - 侧边栏导航 + 面包屑
  - 404/500 错误页面
```

### Phase 1 — 核心业务页面（第 3-6 周）

```
□ Week 3   仪表盘
  - StatCard 组件 (销售额/订单/转化/广告)
  - SalesChart (Recharts 折线图)
  - TopProducts 表格
  - 数字滚动动画 + 骨架屏加载态

□ Week 4   商品管理
  - 商品列表页 (DataTable + 搜索/筛选/排序/分页)
  - 商品详情页
  - AI Listing 生成器 (StepWizard)
    - Step 1: 产品信息表单
    - Step 2: AI 生成中状态 (SSE 流式)
    - Step 3: 富文本编辑 + 评分
    - Step 4: 发布确认

□ Week 5   订单管理
  - 订单列表页（聚合展示）
  - 订单详情页
  - 批量操作栏（打单/发货）
  - 物流追踪时间线

□ Week 6   数据分析 + 设置
  - 分析页（日期选择器 + 报表）
  - 设置页（店铺绑定/授权管理）
  - 全局搜索面板 (⌘K Command Palette)
```

### Phase 2 — 打磨上线（第 7-8 周）

```
□ 性能优化
  - Bundle 分析 + 代码分割优化
  - 图片/字体优化
  - Lighthouse 评分 > 90

□ 测试
  - 核心流程单元测试 (Vitest)
  - 核心流程 E2E (Playwright)
  - 无障碍审计 (axe-core)

□ 部署
  - Dockerfile + docker-compose
  - CI/CD Pipeline (GitHub Actions)
  - Staging 环境上线
  - 生产环境上线
```

---

## 十一、待确认事项

在正式开发前，请确认以下关键决策：

1. **UI 风格方向**：专业商务风(深蓝/白) vs 现代极简风(灰白+点缀色)？
2. **认证方案**：NextAuth.js vs 自研JWT方案 vs Auth0/Clerk等第三方？
3. **部署目标**：AWS ECS Fargate vs Vercel vs 自建服务器？
4. **移动端策略**：MVP 阶段 Responsive Web 即可 vs 同步开发 React Native App？
5. **多语言启动时机**：MVP 仅中文 vs 中英双语 vs 仅英文面向国际？
6. **AI Listing 生成**：是否使用 SSE (Server-Sent Events) 流式展示生成过程？

---

> 📌 **下一步**：请审阅以上方案，确认待确认事项后，我立即开始脚手架搭建与代码实现。
